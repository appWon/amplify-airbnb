import styled from 'styled-components'

import { MapButton } from '../../atoms/MapButton'

export const SpinnerContainer = styled(MapButton)`
    position: absolute;
    padding: 2px 15px;
    width: auto;
    height: 35px;
    left: 50%;
    transform: translate(-50%, 0);
`
